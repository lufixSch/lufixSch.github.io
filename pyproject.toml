[project]
name = "homepage"
version = "1.0.0"
description = "Personal Homepage"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-frontmatter",
    "regex",
    "requests"
]
